const USER_ARGUMENTS_NUMBER = 2;
const EXIT_CODES = {
  success: 0,
  failure: 1
} 

const ERROR_MESSAGES = {
  NOT_FOUND: 'Not found'
}

module.exports = {
  USER_ARGUMENTS_NUMBER,
  EXIT_CODES,
  ERROR_MESSAGES
}
